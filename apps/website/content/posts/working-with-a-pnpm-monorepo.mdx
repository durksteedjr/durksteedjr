---
date: "07/21/2023"
description: "Maintaining a public CLI, colors, UI, and website in a PNPM monorepo."
title: "Working With a PNPM Monorepo"
---

# {frontmatter?.title}

Maintaining a public CLI, colors, UI, and website has been significantly more manageable after migrating to a PNPM monorepo.

[My PNPM Monorepo](https://github.com/durksteedjr/monorepo).

## PNPM Monorepo

Because my public NPM packages are tightly integrated, keeping them co-located within a single repository greatly simplifies maintenance. PNPM has built-in support for monorepos with [PNPM Workspaces](https://pnpm.io/workspaces).

## Turborepo

[Turborepo](https://turbo.build) is an incremental build system and performant caching solution for JavaScript and TypeScript monorepos. Combined with PNPM, my monorepo is capable of incrementally caching `package.json` scripts while running in parallel execution, providing an incredibly smooth developer experience.

For example, consider the following Turborepo configuration:

```json
{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
    // ...
  }
}
```

Turborepo can now run `build` for all monorepo packages in parallel execution while incrementally caching the output files saved in `dist/**`:

```sh
pnpm turbo run build
```

## Changesets

[Changesets](https://github.com/changesets/changesets) manages package changelogs, publishing, and versioning. The [Changesets Bot](https://github.com/apps/changeset-bot) combined with GitHub Actions automates the NPM workflow for all packages in my PNPM monorepo.

## Structure

Below is an outline of my PNPM monorepo's structure:

```md
├── apps
│ ├── cli
│ ├── colors
│ ├── ui
│ ├── website
├── configs
| ├── moonlitii
│ ├── tailwindcss
│ ├── tsconfig
├── durksteedjr
| ├── cli
│ ├── colors
│ ├── ui
├── services
│ ├── db
│ ├── sdk
└─└── ui
```

### @apps/cli

[Next.js](https://nextjs.org), [React.js](https://react.dev) website for `@durksteedjr/cli`.

### @apps/colors

[Next.js](https://nextjs.org), [React.js](https://react.dev) website for `@durksteedjr/colors`.

### @apps/ui

[Next.js](https://nextjs.org), [React.js](https://react.dev), [Storybook.js](https://storybook.js.org) website for `@durksteedjr/ui`.

### @apps/website

[Next.js](https://nextjs.org), [React.js](https://react.dev) website.

### @configs/moonlitii

[Moonlit II.](https://github.com/atomiks/moonlight-vscode-theme) shared syntax highlighting theme.

### @configs/tailwindcss

[Tailwind CSS](https://tailwindcss.com) shared preset.

### @configs/tsconfig

[TypeScript](https://www.typescriptlang.org) shared config.

### @durksteedjr/cli

[@durksteedjr/cli](https://www.npmjs.com/package/@durksteedjr/cli) NPM package.

### @durksteedjr/colors

[@durksteedjr/colors](https://www.npmjs.com/package/@durksteedjr/colors) NPM package.

### @durksteedjr/ui

[@durksteedjr/ui](https://www.npmjs.com/package/@durksteedjr/ui) NPM package.

### @services/db

Internal package containing [Prisma](https://www.prisma.io) types for MySQL [PlanetScale](https://planetscale.com) database.

### @services/sdk

Internal sdk package.

### @services/ui

Internal UI package.
