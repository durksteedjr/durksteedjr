import { OverlayGrid } from "@durksteedjr/ui";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof OverlayGrid> = {
  component: OverlayGrid,
  title: "Theme/OverlayGrid"
};

export default meta;

type Story = StoryObj<typeof OverlayGrid>;

export const Default: Story = {};
