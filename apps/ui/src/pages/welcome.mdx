import {
  Grid,
  H1,
  H2,
  LinkBadge,
  P,
  Snippet,
  Wrapper
} from "@durksteedjr/ui";
import { links } from "@internal/sdk";
import { Meta } from "@storybook/blocks";

import { links as libLinks } from "../lib";

<Meta title="Welcome" />

<Wrapper
  className="sb-unstyled"
  style={{ fontFamily: "Nunito Sans" }}
>
  <Grid className="!mb-6 gap-4 sm:grid-cols-2 md:!mb-8">
    {[libLinks.gitHub, libLinks.npm].map((link) => (
      <LinkBadge
        href={link.href}
        isOutlineArrowUpRight
        key={link.text}
        target="_blank"
      >
        {link.text}
      </LinkBadge>
    ))}
  </Grid>
  <H1>@durksteedjr/ui</H1>
  <H2 id="installation">Installation</H2>
  <Snippet>pnpm i @durksteedjr/ui</Snippet>
  <H2 id="usage">Usage</H2>
  <Snippet>{`import { Badge } from "@durksteedjr/ui"`};</Snippet>
  <Grid className="!mt-24 gap-4 sm:grid-cols-2 md:!mt-32">
    {[links.durkSteedJr, libLinks.mitLicense].map((link) => (
      <LinkBadge
        href={link.href}
        isOutlineArrowUpRight
        key={link.text}
        target="_blank"
      >
        {link.text}
      </LinkBadge>
    ))}
  </Grid>
</Wrapper>
